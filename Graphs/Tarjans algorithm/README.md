### Tarjans algorithm

This algorithm is also used to find scc i.e. strongly connected components.
This algorithm is a bit better than kosaraju's algorithm as here we are not doing multiple dfs.
Also this algorithm works with undirected graph as well as we are not dependent on direction of edges here.
